<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:U2AlumnosApp.ViewModels"
             x:Class="U2AlumnosApp.Views.Login" x:Name="view1">


    <ContentPage.BindingContext>
        <vm:InicioSesionViewModel></vm:InicioSesionViewModel>
    </ContentPage.BindingContext>

    <StackLayout  BackgroundColor="White">
        <Image HorizontalOptions="Center" HeightRequest="80" Margin="0,80,0,20" Source="Buho"></Image>
        <Label HorizontalOptions="Center" FontSize="Large" FontAttributes="Bold">Iniciar Sesion</Label>
        <StackLayout Margin="50, 40,40, 40" >
        <Label Margin="0, 0, 0, -10">Clave</Label>
        <Entry Text="{Binding Clave}"></Entry>
        <Label Margin="0, 20,0,-10" >Constraseña</Label>
        <Entry IsPassword="True" Text="{Binding Password}" ></Entry>
        <Label Text="{Binding Error}" TextColor="Red"></Label>
        <Button BackgroundColor="#29B773" Command="{Binding IniciarCommand}"  TextColor="White" Text="Iniciar" Margin="0, 20, 0, 0"></Button>
        </StackLayout>
    </StackLayout>
</ContentPage>